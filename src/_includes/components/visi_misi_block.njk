<section class="content-block">
    <div class="container">
        <h2 class="block-title">Visi & Misi</h2>
        
        <!-- Visi -->
        <div class="card" style="margin-bottom: 2rem;">
            <h3 style="color: var(--accent); text-align: center; margin-bottom: 1rem;">Visi</h3>
            <p style="text-align: center; font-size: 1.1rem; font-style: italic;">{{ school.visi }}</p>
        </div>
        
        <!-- Misi -->
        <div class="card">
            <h3 style="color: var(--accent); text-align: center; margin-bottom: 1.5rem;">Misi</h3>
            <ul style="list-style: none; padding: 0;">
                {% for item in school.misi %}
                    <li style="margin-bottom: 1rem; padding-left: 2rem; position: relative;">
                        <span style="position: absolute; left: 0; color: var(--accent);">✓</span>
                        {{ item }}
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
</section>